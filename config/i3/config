# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

####################################
##                                ##
##  AUTHOR: SHEIKH SAAD ABDULLAH  ##
##                                ##
####################################



#################
##             ##
##  AUTOSTART  ##
##             ##
#################
# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
#exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
exec --no-startup-id light-locker

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

# personal autostart applications
exec nitrogen --restore
exec --no-startup-id xfce4-panel --disable-wm-check
exec_always --no-startup-id picom --experimental-backends
exec_always --no-startup-id alternating_layouts.py
exec ibus-daemon -drx
exec redshift-gtk
exec --no-startup-id xiccd
exec start-pulseaudio-x11
exec xfce4-power-manager
exec xfsettingsd
exec uget-gtk
#exec_always --no-startup-id $HOME/.config/i3/polybar.sh &
#exec xfce4-clipman
#exec blueman-applet
#exec xfdashboard -d
#exec java -Xmx1024m -jar "/opt/xdman/xdman.jar" -m
#exec nextcloud --background
#exec_always --no-startup-id kitti3



###################
##               ##
##  KEY ALIASES  ##
##               ##
###################
set $mod mod1  # ALT key
set $sup mod1+F1  # Super key
set $alter Multi_key  # Alt-Right key



###################
##               ##
##  KEYBINDINGS  ##
##               ##
###################
# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
# start a terminal
bindsym $mod+Shift+t exec kitty
#bindsym $mod+Shift+t exec terminology
# personal keybindings
# bindsym $sup+Control exec rofi -modi "Global Search":"/home/sage/.config/rofi/rofi-spotlight.sh" -show "Global Search" -config /home/sage/.config/rofi/rofi.rasi
bindsym $sup exec xfce4-appfinder --collapsed
bindsym $mod+Control+M exec xfce4-taskmanager
bindsym $mod+Control+P exec xfce4-display-settings --minimal
bindsym $mod+Control+Delete exec xflock4
bindsym Print exec xfce4-screenshooter -fmd 1
bindsym $mod+Control+A exec xfce4-screenshooter -r
bindsym Shift+Print exec xfce4-screenshooter -wd 1
#bindsym $mod+Control+X exec xkill
#bindsym Print exec scrot -zp "%a-%d-%b-%s.png" -e 'mv $f ~/Pictures/Screenshots/'
#bindsym $mod+Control+A exec scrot -zs "%a-%d-%b-%s.png" -e 'xclip -selection clipboard -t image/png -i $f'
#bindsym Shift+Print exec scrot -zub "%a-%d-%b-%s.png" -e 'xclip -selection clipboard -t image/png -i $f'
#bindsym Print exec maim ~/Pictures/Screenshots/$(date +%a-%d-%b-%s).png
#bindsym $mod+Control+A exec maim -us | xclip -selection clipboard -t image/png
#bindsym Shift+Print exec maim -ui $(xdotool getactivewindow) | xclip -selection clipboard -t image/png
# kitty dropdown
exec_always --no-startup-id $HOME/.config/i3/kitti3
for_window [instance="kitty-dropdown"] floating enable, border none, move absolute position 273px 32px, resize set 820px 365px, move scratchpad
bindsym $mod+t [instance="kitty-dropdown"] scratchpad show

# terminology dropdown
#exec_always --no-startup-id $HOME/.config/i3/termi3
#for_window [instance="term-dropdown"] floating enable, border none, move absolute position 273px 32px, resize set 820px 365px, move scratchpad
#bindsym $mod+t [instance="term-dropdown"] scratchpad show

# start dmenu (a program launcher)
#bindsym $mod+d exec --no-startup-id dmenu_run -b
# A more modern dmenu replacement is rofi:
# bindcode $mod+40 exec "rofi -modi drun,run -show drun"
# There also is i3-dmenu-desktop which only displays applications shipping a
# .desktop file. It is a wrapper around dmenu, so you need that installed.
# bindcode $mod+40 exec --no-startup-id i3-dmenu-desktop



###########################
##                       ##
##  WINDOW CONTROL KEYS  ##
##                       ##
###########################
# kill focused window
bindsym $mod+Shift+q kill

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        bindsym Left resize shrink width 10 px or 10 ppt
        # Pressing right will grow the window’s width.
        bindsym Right resize grow width 10 px or 10 ppt
        # Pressing up will shrink the window’s height.
        bindsym Up resize shrink height 10 px or 10 ppt
        # Pressing down will grow the window’s height.
        bindsym Down resize grow height 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"



###################
##               ##
##  i3 SETTINGS  ##
##               ##
###################
# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"



##############
##          ##
##  i3 BAR  ##
##          ##
##############
# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        i3bar_command i3bar --transparency
        height 24
        status_command exec $HOME/.config/i3/my3status
#        status_command exec i3status -c $HOME/.config/i3/i3status.conf
        position bottom
        strip_workspace_numbers yes
        # BAR AESTHETICS
        colors {
                background         #69696969
                statusline         #efefef
                separator          #666666

                # class            border  background text
                focused_workspace  $NORDIC $NORD   #ffffff
                active_workspace   #333333 #ba9eba #ffffff
                inactive_workspace #333333 #222222 #888888
                urgent_workspace   #2f343a #900000 #ffffff
                binding_mode       #2f343a #900000 #ffffff
        }
}



##################
##              ##
##  WORKSPACES  ##
##              ##
##################
# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws0 "0"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws0

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws0



############################
##                        ##
##  WORKSPACE MANAGEMENT  ##
##                        ##
############################
# assign windows to specific workspaces based on type
assign [class="code"] $ws1
assign [class="kitty"] $ws1
assign [class="Navigator"] $ws2
#assign [class=""] $ws4
#assign [class=""] $ws5
#assign [class=""] $ws6
#assign [class=""] $ws7
#assign [class=""] $ws8
#assign [class=""] $ws9
#assign [class=""] $ws0

# force move windows where assign doesn't work
for_window [title="GitKraken"] move container to workspace $ws1
for_window [title="Brave"] move container to workspace $ws3



####################
##                ##
##  WINDOW RULES  ##
##                ##
####################
# disable titlebar and enable border
for_window [instance="plank"] floating enable
for_window [instance="xfce4-appfinder"] floating enable
for_window [instance="xfce4-panel"] floating enable
for_window [instance="xfce4-power-manager"] floating enable
for_window [instance="pavucontrol"] floating enable
#for_window [instance="pamac"] floating enable
#for_window [instance="xdman"] floating enable
for_window [instance="galculator"] floating enable
for_window [instance="gcolor3"] floating enable
for_window [instance="simplescreenrecorder"] floating enable
for_window [instance="nitrogen"] floating enable
for_window [title="Preferences$"] floating enable
for_window [class=".*"] border pixel 4

# enable tabs in surf browser
for_window [class="surf"] focus child, layout tabbed, focus



##################
##              ##
##  AESTHETICS  ##
##              ##
##################
# enable gaps
gaps inner 8
#gaps outer 3
smart_gaps on
smart_borders on
popup_during_fullscreen smart

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:SF Pro Text 11

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# COLOR CODES
set $PINK   #d4add4
set $WHAT   #ffbeff
set $MEHK   #bb7ebb
set $NORD   #576675
set $NORDIC #788490

# DarkGrey background with white text - not focus grey background - black text
# Windows decoration colors
# class                 border  backgr. text    indicator
client.placeholder      #242424 #242424 #242424
client.background       #242424 #242424 #242424
client.focused          $NORD   $NORD   #e5e5e5 $NORD
client.unfocused        #222222 #222222 #aaaaaa #222222
client.focused_inactive #222222 #222222 #a9a9a9 #222222
client.urgent           #8AC4AC #8AC4AC #f7f7f7 #8AC4AC

